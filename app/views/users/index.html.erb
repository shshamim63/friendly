<% @user.each do |user| %>
  <%= user.full_name %><br>
  <% if current_user.friend?(user) %>
    <%= link_to 'Unfriend', unfriend_user_friendships_path(user_id: user.id), method: :post %><br><br>
  <% else %>
  
    <%= link_to 'Add', add_user_friendships_path(user_id: user.id), method: :post %><br><br>
  <% end %>
<% end %>
